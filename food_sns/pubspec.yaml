name: food_sns
description: "위치기반 정보 공유 서비스"
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.4.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0
  flutter_naver_map: ^1.2.2+flutter3.22
  geolocator: ^12.0.0
  image_picker: ^1.1.2
  supabase_flutter: ^2.5.6
  daum_postcode_search: ^0.0.2
  intl: ^0.19.0
  flutter_inappwebview: ^5.8.0
  http: ^1.2.2
  logger: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  assets:
    - assets/
    - .env
  uses-material-design: true
